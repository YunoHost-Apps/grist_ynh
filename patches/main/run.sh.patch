diff --git a/sandbox/run.sh b/sandbox/run.sh
index 8bccf4ac9..38ec741eb 100755
--- a/sandbox/run.sh
+++ b/sandbox/run.sh
@@ -6,7 +6,10 @@ if [[ "$GRIST_SANDBOX_FLAVOR" = "gvisor" ]]; then
   source ./sandbox/gvisor/get_checkpoint_path.sh
 
   # Check GVISOR_FLAGS we ended up with. Don't ignore the output, it may be helpful in troubleshooting.
-  if runsc --network none $GVISOR_FLAGS "do" true; then
+  # When running in systemd, even with many privileges, runsc crashes. No log available to explain this crash.
+  # Weirder, when using systemd-cat (which just redirects the output to the systemd journal) to run runsc, it
+  # works as expected. Freaking weird…
+  if systemd-cat runsc --network none $GVISOR_FLAGS "do" true; then
     echo "gvisor check ok (flags: ${GVISOR_FLAGS})"
   else
     echo "gvisor check failed (flags: ${GVISOR_FLAGS}); consider different GVISOR_FLAGS or GRIST_SANDBOX_FLAVOR"
